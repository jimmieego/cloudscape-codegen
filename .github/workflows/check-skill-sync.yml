name: Check skill sync

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  check-skill-sync:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check root and Kiro skill match
        run: npm run check:skill
